# GitHub Pages Jekyll Configuration
title: High Intent Signals
description: Daily scanner for high-conviction technical breakouts
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme

# Build settings
markdown: kramdown
highlighter: rouge

# Include data directory
include:
  - _data

# Exclude from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock

# Optional: Custom domain
# url: "https://yourdomain.com"
